site_name: arxiv2026_grep
nav:
  - Home: index.md

theme:
  name: material

plugins:
  - search
  - mkdocstrings:
      project_name: arxiv2026_grep
      handlers:
        python:
          options:
            docstring_style: numpy